name: Build Waveshare LED Display

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.1

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            ccache \
            cmake \
            git \
            ninja-build \
            gcc-arm-none-eabi \
            libnewlib-arm-none-eabi \
            libusb-1.0-0-dev

      - name: Cache Pico SDK
        id: cache-pico-sdk
        uses: actions/cache@v6
        with:
          path: .pico-sdk
          key: ${{ runner.os }}-pico-sdk-2.2.0

      - name: Cache ccache
        uses: actions/cache@v6
        with:
          path: ~/.ccache
          key: ${{ runner.os }}-ccache-pico2w-${{ hashFiles('CMakeLists.txt', 'common/CMakeLists.txt', 'libraries/CMakeLists.txt', 'src/**', 'examples/**', 'common/**', 'libraries/**', 'hub75_demo.cpp') }}
          restore-keys: |
            ${{ runner.os }}-ccache-pico2w-

      - name: Checkout Pico SDK (with submodules)
        if: steps.cache-pico-sdk.outputs.cache-hit != 'true'
        run: |
          git clone --depth 1 --branch 2.2.0 --recurse-submodules https://github.com/raspberrypi/pico-sdk.git "$GITHUB_WORKSPACE/.pico-sdk"

      - name: Configure CMake
        env:
          CCACHE_DIR: ~/.ccache
          CCACHE_BASEDIR: ${{ github.workspace }}
          CCACHE_COMPILERCHECK: content
          WIFI_SSID: ${{ vars.WIFI_SSID }}
          WIFI_PASSWORD: ${{ secrets.WIFI_PASSWORD }}
          NTP_SERVER: ${{ vars.NTP_SERVER }}
        run: |
          NTP_SERVER_VALUE="${NTP_SERVER:-fritz.box}"
          SSID_VALUE="${WIFI_SSID:-<unset>}"
          SSID_LEN="${#WIFI_SSID}"
          echo "CI config: WIFI_SSID=${SSID_VALUE}"
          echo "CI config: WIFI_SSID length=${SSID_LEN}"
          echo "CI config: NTP_SERVER=${NTP_SERVER_VALUE}"
          cmake -S . -B build-ci -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DPICO_BOARD=pico2_w \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
            -DWIFI_SSID="$WIFI_SSID" \
            -DWIFI_PASSWORD="$WIFI_PASSWORD" \
            -DNTP_SERVER="$NTP_SERVER_VALUE" \
            -DPICO_SDK_PATH="$GITHUB_WORKSPACE/.pico-sdk"

      - name: Build
        run: cmake --build build-ci --parallel

      - name: ccache stats
        run: ccache --show-stats

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v7
        with:
          name: hub75-pico2w
          path: |
            build-ci/*.uf2
            build-ci/*.elf
            build-ci/*.bin
          if-no-files-found: error
